{% extends 'base.html.twig' %}

{% block title %}Message{% endblock %}

{% block body %}
    <h1>Message</h1>
    <pre>
      
    </pre>
    <div class="fw-bold">
    {% for message in messagesone %}
        {{ message.ToFreelancer.firstname ~ ' ' ~ message.ToFreelancer.lastname }}
        {{ message.subject }}
        {{ message.message }}
        {{ message.sentdate|date('d/m/Y') }}
    {% endfor %}
      </div>
    {% for message in messagestwo %}
        {{ message.FromFreelancer.firstname ~ ' ' ~ message.FromFreelancer.lastname }}
        {{ message.subject }}
        {{ message.message }}
        {{ message.sentdate|date('d/m/Y') }}
    {% endfor %}
       
       
    
    
    {# <table class="table">
    
        <tbody>
            
            <tr>
                <th>Subject</th>
                <td>{{ message.subject }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ message.ToFreelancer }}</td>
            </tr>
            <tr>
                <th>Sentdate</th>
                <td>{{ message.sentdate ? message.sentdate|date('Y-m-d') : '' }}</td>
            </tr>

             <tr>
                <th>Subject</th>
                <td>{{ message.subject }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ message.FromFreelancer }}</td>
            </tr>
            <tr>
                <th>Sentdate</th>
                <td>{{ message.sentdate ? message.sentdate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table> #}
{# 
    <a href="{{ path('message_index') }}">back to list</a>

    <a href="{{ path('message_edit', {'id': message.id}) }}">edit</a> #}

    {# {{ include('message/_delete_form.html.twig') }} #}
{% endblock %}
